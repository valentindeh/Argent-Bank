export type UserInfos = {
    id: string
    email: string
    userName: string
    firstName: string
    lastName: string
    createdAt: string
    updatedAt: string
}

export type UsernameUpdate = {
    userName: string
}